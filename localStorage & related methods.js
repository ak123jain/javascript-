// data ko ek particular domain ma store karna ka liye use hoti ha browser me
// in cookies ma hmna agar cookie set kar de to browser aapki har request par 
// cookie ko send karega to web server
// esliye ham localStorage ka use karna chahege kyuki ham nahi chata ki browser har
// request par cookie send kare to the server

// when we store data in localStorage it survive all the time

//when passed a key name, will return that key's value, or null if the key does not exist, in the given Storage object
// getItem(keyName) pass to key name but return key value

// let keyName = prompt("enter key you want to set")

// let value = prompt("enter value you want to set")

// localStorage.setItem(keyName,value)

// console.log(`the value at ${keyName} is ${localStorage.getItem(keyName)}`)
// print hoone ka baad local storage se remove ho gyegi agar if condition hit hui to

// if(keyName == "red" || keyName == "blue"){
//     localStorage.removeItem(keyName)
// }

// if(keyName == 0){
//     localStorage.clear()
// }
 
 // localStorage.key(0) in console

//${localStorage.getItem(key) return value of key

// remove item => remove the key with its value



 
